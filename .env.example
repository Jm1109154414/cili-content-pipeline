# Este repo ya NO ejecuta llamadas a Claude/OpenAI directamente — ese trabajo
# lo hace OpenClaw a través de los skills en /skills (ver PLAN_MAESTRO.md).
# Las API keys (Claude, OpenAI) se configuran en OpenClaw, no aquí
# (~/.openclaw/openclaw.json en la máquina donde corre OpenClaw).
#
# Este archivo queda solo como referencia histórica — ningún script de este
# repo lee variables de entorno actualmente.
